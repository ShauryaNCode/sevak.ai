# Infrastructure - Docker

## Purpose

Contains container build definitions and local compose assets.

## Responsibilities

- Service image build context
- Local environment orchestration
- Consistent dev and CI runtime definitions
