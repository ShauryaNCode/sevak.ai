# Frontend Feature - Authentication

## Purpose

Owns sign-in, device trust, session restoration, and role-aware entry into the application.

## Future Plan

Add auth entities, repository contracts, BLoC shells, and login presentation flows without embedding provider-specific details into UI code.
